# Flyte 2 RNA-Seq QC + Quant Workflow

The main project documentation now lives in `README.md`.

This file is kept as a compatibility pointer because the implementation has moved from a single file into the `src/flytetest/` package while preserving the original `flyte_rnaseq_workflow.py` entrypoint for `flyte run`.
